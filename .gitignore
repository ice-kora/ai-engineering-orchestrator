# --- Python ---
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# --- Secrets / env (never commit real credentials) ---
.env
*.pem
*.key

# --- Sandbox: demo-repo is an independent git repository managed by itself ---
sandbox/demo-repo/
# --- Sandbox: headless probe scratch & agent-mail test data (regenerable) ---
sandbox/zcode-headless-scratch/
sandbox/agent-mail-test-data/
probe*-stderr.txt

# --- Logs / transient ---
*.log
docs/p0-evidence/*.raw.txt

# --- OS ---
Thumbs.db
.DS_Store
.zcode/

# --- Local-only security evidence (never push) ---
local/
sandbox/bd-server-probe/
sandbox/agy-scratch/
